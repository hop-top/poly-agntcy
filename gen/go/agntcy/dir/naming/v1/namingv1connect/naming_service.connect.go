// Copyright AGNTCY Contributors (https://github.com/agntcy)
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: agntcy/dir/naming/v1/naming_service.proto

package namingv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "hop.top/agntcy/gen/go/agntcy/dir/naming/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// NamingServiceName is the fully-qualified name of the NamingService service.
	NamingServiceName = "agntcy.dir.naming.v1.NamingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// NamingServiceGetVerificationInfoProcedure is the fully-qualified name of the NamingService's
	// GetVerificationInfo RPC.
	NamingServiceGetVerificationInfoProcedure = "/agntcy.dir.naming.v1.NamingService/GetVerificationInfo"
	// NamingServiceResolveProcedure is the fully-qualified name of the NamingService's Resolve RPC.
	NamingServiceResolveProcedure = "/agntcy.dir.naming.v1.NamingService/Resolve"
)

// NamingServiceClient is a client for the agntcy.dir.naming.v1.NamingService service.
type NamingServiceClient interface {
	// GetVerificationInfo retrieves the verification info for a record.
	GetVerificationInfo(context.Context, *connect.Request[v1.GetVerificationInfoRequest]) (*connect.Response[v1.GetVerificationInfoResponse], error)
	// Resolve resolves a record reference (name with optional version) to CIDs.
	// Supports Docker-style references:
	//   - "name" -> returns all versions (newest first)
	//   - "name:version" -> returns the specific version
	//   - "name@cid" -> hash-verified lookup (latest version)
	//   - "name:version@cid" -> hash-verified lookup (specific version)
	//
	// Returns an error if no matching record is found.
	Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error)
}

// NewNamingServiceClient constructs a client for the agntcy.dir.naming.v1.NamingService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewNamingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) NamingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	namingServiceMethods := v1.File_agntcy_dir_naming_v1_naming_service_proto.Services().ByName("NamingService").Methods()
	return &namingServiceClient{
		getVerificationInfo: connect.NewClient[v1.GetVerificationInfoRequest, v1.GetVerificationInfoResponse](
			httpClient,
			baseURL+NamingServiceGetVerificationInfoProcedure,
			connect.WithSchema(namingServiceMethods.ByName("GetVerificationInfo")),
			connect.WithClientOptions(opts...),
		),
		resolve: connect.NewClient[v1.ResolveRequest, v1.ResolveResponse](
			httpClient,
			baseURL+NamingServiceResolveProcedure,
			connect.WithSchema(namingServiceMethods.ByName("Resolve")),
			connect.WithClientOptions(opts...),
		),
	}
}

// namingServiceClient implements NamingServiceClient.
type namingServiceClient struct {
	getVerificationInfo *connect.Client[v1.GetVerificationInfoRequest, v1.GetVerificationInfoResponse]
	resolve             *connect.Client[v1.ResolveRequest, v1.ResolveResponse]
}

// GetVerificationInfo calls agntcy.dir.naming.v1.NamingService.GetVerificationInfo.
func (c *namingServiceClient) GetVerificationInfo(ctx context.Context, req *connect.Request[v1.GetVerificationInfoRequest]) (*connect.Response[v1.GetVerificationInfoResponse], error) {
	return c.getVerificationInfo.CallUnary(ctx, req)
}

// Resolve calls agntcy.dir.naming.v1.NamingService.Resolve.
func (c *namingServiceClient) Resolve(ctx context.Context, req *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error) {
	return c.resolve.CallUnary(ctx, req)
}

// NamingServiceHandler is an implementation of the agntcy.dir.naming.v1.NamingService service.
type NamingServiceHandler interface {
	// GetVerificationInfo retrieves the verification info for a record.
	GetVerificationInfo(context.Context, *connect.Request[v1.GetVerificationInfoRequest]) (*connect.Response[v1.GetVerificationInfoResponse], error)
	// Resolve resolves a record reference (name with optional version) to CIDs.
	// Supports Docker-style references:
	//   - "name" -> returns all versions (newest first)
	//   - "name:version" -> returns the specific version
	//   - "name@cid" -> hash-verified lookup (latest version)
	//   - "name:version@cid" -> hash-verified lookup (specific version)
	//
	// Returns an error if no matching record is found.
	Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error)
}

// NewNamingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewNamingServiceHandler(svc NamingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	namingServiceMethods := v1.File_agntcy_dir_naming_v1_naming_service_proto.Services().ByName("NamingService").Methods()
	namingServiceGetVerificationInfoHandler := connect.NewUnaryHandler(
		NamingServiceGetVerificationInfoProcedure,
		svc.GetVerificationInfo,
		connect.WithSchema(namingServiceMethods.ByName("GetVerificationInfo")),
		connect.WithHandlerOptions(opts...),
	)
	namingServiceResolveHandler := connect.NewUnaryHandler(
		NamingServiceResolveProcedure,
		svc.Resolve,
		connect.WithSchema(namingServiceMethods.ByName("Resolve")),
		connect.WithHandlerOptions(opts...),
	)
	return "/agntcy.dir.naming.v1.NamingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case NamingServiceGetVerificationInfoProcedure:
			namingServiceGetVerificationInfoHandler.ServeHTTP(w, r)
		case NamingServiceResolveProcedure:
			namingServiceResolveHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedNamingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedNamingServiceHandler struct{}

func (UnimplementedNamingServiceHandler) GetVerificationInfo(context.Context, *connect.Request[v1.GetVerificationInfoRequest]) (*connect.Response[v1.GetVerificationInfoResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agntcy.dir.naming.v1.NamingService.GetVerificationInfo is not implemented"))
}

func (UnimplementedNamingServiceHandler) Resolve(context.Context, *connect.Request[v1.ResolveRequest]) (*connect.Response[v1.ResolveResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("agntcy.dir.naming.v1.NamingService.Resolve is not implemented"))
}
