{
  "name": "poly-agntcy-ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6",
    "vitest": "^2.1",
    "@types/node": "^22"
  },
  "packageManager": "pnpm@9.12.0"
}
