services:
  dir:
    image: ghcr.io/agntcy/dir-apiserver:latest
    ports:
      - "8888:8888"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8888/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
